//a simple program that has variables
#include <stdio.h>
int main()
{
  int x; 
  char y; //
  float z; //
  double t; //
  printf("hello world…\n");
  printf("size of int %d, size of char %d, size of float %d and size of double %d\n", sizeof (int), sizeof(char), sizeof(float), sizeof(double));
  return 0;
}
